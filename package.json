{
  "name": "data-cleansing",
  "private": true,
  "version": "0.1.0",
  "description": "AI 数据采集与智能清洗平台 — 表格数据清洗（web）+ 图片/音频/视频结构化（基于火山引擎 API）",
  "workspaces": [
    "bff"
  ],
  "scripts": {
    "dev": "concurrently -n bff,web -c cyan,magenta \"npm:dev:bff\" \"npm:dev:web\"",
    "dev:bff": "npm --workspace bff run dev",
    "dev:web": "npm --prefix web run dev",
    "install:web": "npm --prefix web install",
    "build": "npm --workspace bff run build && npm --prefix web run build",
    "typecheck": "npm --workspace bff run typecheck && npm --prefix web run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
