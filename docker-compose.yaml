name: data-cleansing

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: data_cleansing
    ports:
      - "5432:5432" # 本地不跑 bff 容器、直接 npm run dev 调试时需要连到宿主机端口，可删
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    mem_limit: 512m
    restart: unless-stopped

  # 演示用"外部数据源"：与应用自己的 db 完全独立，只用来在"数据库连接器"演示时
  # 现场连接、拉取黑龙江省真实数据（4 张表，见 demo-data/heilongjiang-init.sql 顶部的数据来源说明）。
  demo-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: demo
      POSTGRES_PASSWORD: ${DEMO_DB_PASSWORD:-demo12345}
      POSTGRES_DB: heilongjiang
    ports:
      # 注意两种场景端口不一样：
      # - 整套用 docker compose 起（bff 也是容器）：连接器里填 host=demo-db port=5432（同一 compose 网络内部访问）
      # - 本机直接 npm run dev 调试 bff（不进容器）：连接器里填 host=localhost port=5434（下面这条宿主机映射）
      - "5434:5432"
    volumes:
      - ./demo-data:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U demo"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    mem_limit: 256m
    restart: unless-stopped

  # 演示用"外部 API + 演示网页"：轻量零依赖 Node 服务，内嵌黑龙江省真实年度序列数据
  # （对俄贸易 / 大庆油田，见 demo-api/data.json 的来源说明），演示"API 连接器"的
  # API Key 鉴权（X-Api-Key）和页码翻页；同时托管 /page/bulletin.html 静态公报页，
  # 供"URL / 网页"来源演示真实抓取与表格解析。端口两种场景同理：
  # - 容器网内（bff 抓网页/调 API）：http://demo-api:8090
  # - 本机裸跑 bff 调试：http://localhost:8090
  demo-api:
    build:
      context: ./demo-api
      dockerfile: Dockerfile
    image: data-cleansing-demo-api
    environment:
      DEMO_API_KEY: ${DEMO_API_KEY:-hlj-demo-2026}
    ports:
      - "8090:8090"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:8090/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    mem_limit: 128m
    restart: unless-stopped

  bff:
    build:
      context: .
      dockerfile: bff/Dockerfile
    image: data-cleansing-bff
    # 全部火山密钥从仓库根 .env 注入（不进镜像）
    env_file:
      - .env
    environment:
      BFF_PORT: "8787"
      # 浏览器实际访问的地址（nginx 端口），用于 CORS
      WEB_ORIGIN: "http://localhost:8080"
      FFMPEG_PATH: /usr/bin/ffmpeg
      DATABASE_URL: postgres://postgres:${POSTGRES_PASSWORD:-postgres}@db:5432/data_cleansing
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8787:8787" # 直连调试用，可删
    volumes:
      - ./exports:/app/exports # 数仓回传落盘
      - ./telemetry:/app/telemetry # 埋点落盘
      - ./uploads:/app/uploads # 原始素材落盘，历史回看 / 任务重启续跑用
      - ./jobs:/app/jobs # 后台任务槽心跳，容器重建后能接着跑
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://localhost:8787/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    mem_limit: 1536m
    restart: unless-stopped

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    image: data-cleansing-web
    ports:
      - "8080:80" # 打开 http://localhost:8080
    depends_on:
      bff:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s
    mem_limit: 128m
    restart: unless-stopped
